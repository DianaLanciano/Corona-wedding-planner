@model IEnumerable<CoronaWedding.Models.Photographer>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.supplierEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.supplierEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.price)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PhotographerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PhotographerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PhotographerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
